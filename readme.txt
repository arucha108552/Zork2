To build the jar file is run "$ mvn package" on the terminal on the root directory
To Start the game run the terminal command "$ java -cp target/zork-1.0-SNAPSHOT.jar io.muic.ooc.thienthai.zork.startGame" on root directory
