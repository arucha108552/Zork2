1. Ignore .idea because the developer might want to use different
specific setting in there project
2. /target because when we build the maven it will create the 
target file automatically so it is no need to add target again
3. /*.DS_Store it not use for the project and it difference setting in each machine 
